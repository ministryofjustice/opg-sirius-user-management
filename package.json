{
  "name": "opg-sirius-user-management",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "yarn --cwd prototype install",
    "start:prototype": "yarn --cwd prototype start",
    "test": "yarn --cwd prototype test",
    "copy-assets": "rm -rf web/static/public && cp -R prototype/public web/static/public && rm -rf web/static/public/sass"
  }
}
